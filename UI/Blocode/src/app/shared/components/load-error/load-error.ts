import { NgClass } from '@angular/common';
import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-load-error',
  imports: [],
  templateUrl: './load-error.html',
  styleUrl: './load-error.css',
})
export class LoadError {
}
